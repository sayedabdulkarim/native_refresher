import { StyleSheet, Text, View } from "react-native";
import React from "react";
import PlaceForm from "../../component/featuresApp/PlaceForm";

const AddPlace = () => {
  return <PlaceForm />;
};

export default AddPlace;

const styles = StyleSheet.create({});
